public enum PlayerState{
    Active, //Player is active, most player inputs are available
    Dashing, //Player is dashing
    Walling, //Player is attached to a wall
    Staggered, //Player is staggered, most player inputs are unavailable
}
